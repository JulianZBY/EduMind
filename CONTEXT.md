# CONTEXT：EduMind 领域词汇表

**用法**：面向教师的文案、界面标签、按钮、提示、报错、文档一律使用本表的术语。
写代码时的内部命名（表名、函数名）不强制，但**读代码后向教师转述**时必须落回本表。
新概念先加词条再用；同一概念出现两种叫法时当次统一（见 `docs/agents/domain.md`）。

本文只回答「这个词是什么意思」，不回答「怎么实现」；实现与决策见 `docs/architecture.md` 与 `docs/adr/`。

---

## 1. 六区与设置

工作台按区组织，一区一件事。**区名是本表的正式术语**，界面标题、导航、文档一律照用。

| 区 | 教师在这里做的事 | 一句话定义 |
| --- | --- | --- |
| **备课会话** | 发起备课、回答追问、看生成结果 | 一次备课的完整对话现场，默认落脚区 |
| **知识库** | 上传与管理教学资料 | 教师个人教学资料的仓库 |
| **生成物** | 预览 / 下载 / 提修改意见 / 看历史版本 | 一次备课产出的课件、教案、提纲、试卷、互动内容的统称与版本中心 |
| **知识图谱** | 看知识点与它们的关系 | 从资料里提炼出的知识点网络 |
| **冲突审核** | 裁决新旧知识矛盾 | 新知识进库前，教师裁决矛盾的地方 |
| **题库** | 浏览题目、按考查知识点筛选 | 可复用的题目资产库 |
| **设置** | 选供应商、填 Key、按任务选模型 | 云端能力与模型档位的配置页（不是业务区） |

**禁用写法**：「产物」一律写作**生成物**；「标签页」不是区，界面上不再有标签页。

## 2. 备课会话

| 术语 | 定义 | 边界 |
| --- | --- | --- |
| **备课会话** | 围绕一节课的一次完整备课过程，包含全部对话与生成物 | 与「一次提问」不同：会话可以持续多轮，跨设备回看 |
| **消息** | 会话里的一条对话内容，分教师说的与助手说的 | — |
| **澄清回复** | 助手发现信息不足时的回复形态：内容是**追问**，不给生成物 | 与生成回复互斥，同一条回复只属于一种形态 |
| **生成回复** | 助手信息够用时的回复形态：给出课件、教案、提纲等生成物 | 生成回复里也会附一句「命中了哪些参考资料」 |
| **追问粒度** | 澄清的强度，三档：**快速 / 标准 / 精细** | 粒度越高追问越细；只在澄清阶段起作用 |
| **跳过追问** | 教师表示「信息够了，直接出结果」（如说要「开始生成」） | 由语义判断，不是关键词匹配；跳过即直接进入生成回复 |
| **参考资料** | 本次备课勾选的、用来影响生成的教学资料 | 与「知识库里的全部资料」不同：只有被勾选的才加权并溯源 |

**易混**：「会话」在本项目里默认指**备课会话**，不是一般聊天的泛称，也不是登录会话（本项目无登录）。

## 3. 生成物

| 术语 | 定义 |
| --- | --- |
| **生成物** | 一次备课产出的可交付文件与内容的统称：课件、教案、提纲、试卷、互动内容 |
| **课件** | 上课用的演示文稿 |
| **教案** | 教学设计文档（目标 / 重点难点 / 过程 / 活动 / 作业） |
| **提纲** | 知识结构纲要 |
| **试卷** | 按备课意图生成的题目集合，题目同时进入题库 |
| **互动内容** | 单文件 HTML5 小游戏或知识点动画，可在浏览器新标签页直接打开试用 |
| **修改意见** | 教师对**某一个**生成物提出的修改要求；只影响所选生成物，不推翻其它结果 |
| **版本** | 某一生成物的一次产出记录；同一生成物在同一会话内每次产出都形成新版本 |
| **当前版本** | 该生成物版本号最高的那一版，是列表与预览默认打开的一版 |
| **历史版本** | 除当前版本以外的全部版本 |
| **基线** | 作为下一次修改出发点的版本 |

### 版本用语（硬约束）

- **当前版本没有特权**：它只是「默认打开的那一版」，不代表其它版本作废。
- **历史版本一律可回看、可下载、可作基线继续修改**；以历史版本为基线修改会产出**版本号更高的新版本**，
  原版本**保持可取回**。
- 同一生成物的全部版本，连同「由哪一版衍生而来」的关系，构成一棵**版本树**；
  「当前版本」只是这棵树上的末端指针。版本号在同一会话内**单调递增**。
- 禁止的表述：「历史文件仅落盘、不展示、不构成版本树」「旧版本被覆盖」「修改即替换原文件」。
  任何与「全版本留痕」相反的措辞都属于冲突，必须在当次改动中修正。

## 4. 知识库与教学资料

| 术语 | 定义 |
| --- | --- |
| **教学资料**（口语可称「资料」「文档」） | 教师上传的 PDF / Word / PPT / 图片 / 视频 / 录音 |
| **解析状态** | 资料从上传到可用的状态：**处理中** → **已完成** / **有冲突** / **失败** |
| **参考资料标记** | 把某份资料标成「备课时优先使用」；标记后检索加权、生成物溯源 |
| **分块** | 把资料切成可检索的小段；一个分块有唯一编号，可回溯到来源资料 |
| **检索** | 按语义找出与当前备课最相关的分块 |

状态口径：**处理中**＝后台还在解析，界面应自动跟进（不需要手动刷新）；
**已完成**＝已入库可检索；**有冲突**＝已入库，但检测出待裁决的矛盾（见冲突审核区）；
**失败**＝解析没有成功，教师可重新上传。

## 5. 知识图谱

| 术语 | 定义 |
| --- | --- |
| **知识点** | 从资料里提炼出的一个可教、可考的知识单元（有内容、学科、章节、难度、来源） |
| **关系** | 知识点之间的四种连线：**前置依赖** / **父子包含** / **推导关系** / **相关关联** |
| **邻域** | 以某个知识点为中心、向外若干跳的子图（看清它的上下游） |
| **来源引用** | 知识点正文来自哪份资料的哪一段 |

## 6. 冲突审核

| 术语 | 定义 |
| --- | --- |
| **冲突** | 新知识与库中已有知识之间的矛盾，进入待审队列等教师裁决 |
| **待审** | 冲突的初始状态：新知**尚未**进入知识图谱 |
| **定义冲突** | 同一个知识点出现两段互相矛盾的描述（「谁对？」） |
| **结构冲突** | 新知识在层级或依赖关系上与图谱现状打架（「图谱该长成什么样？」） |
| **常识存疑** | 内容可能是错的常识，需要教师决定「要不要让它进库」 |
| **三选一** | 定义冲突与结构冲突的裁决动作：**接受新** / **保留旧** / **并存** |
| **接受新** | 用新知替换旧知识点，旧知识点上的关系重挂到新知上 |
| **保留旧** | 丢弃新知，图谱不动 |
| **并存** | 新旧两个知识点都保留，差异说明留在冲突记录里 |
| **两选一** | 常识存疑的裁决动作：**照常入库** / **拒绝** |
| **编辑修正后入库** | 常识存疑的第三条出路：教师改对了内容再入库，**落库的是修正后的内容** |

**文案纪律**：结构冲突与常识存疑的**检测逻辑尚未实现**（见 ADR-0004）；
面向教师的文案与 README 不得承诺能自动发现这两类冲突，只能描述裁决动作与形态。

## 7. 题库

| 术语 | 定义 |
| --- | --- |
| **题目** | 一道可复用的题（题型 / 题干 / 答案 / 解析 / 来源） |
| **题型** | 选择 / 填空 / 简答 / 综合 |
| **考查知识点** | 这道题考的是哪个知识点，以及权重（**主考** / **涉及**）；题库按它筛选 |
| **来源** | 题目从哪来：**自编**（AI 按意图生成）/ **上传** / **网络** |

## 8. 设置与云端能力

| 术语 | 定义 |
| --- | --- |
| **供应商目录** | 可选的服务商清单（如 dashscope / deepseek / 硅基流动），选一家后只需粘贴 Key |
| **自定义 OpenAI 兼容服务** | 供应商目录之外的接入方式：填 `base_url` + 模型 ID |
| **任务级模型** | 按任务分别选模型档位（**意图分析** / **生成** / **冲突比对**）；未设置的任务回落全局默认 |
| **引导默认** | `.env` 里的初始配置；未在设置页改过时生效，改了设置页则以设置页为准 |
| **掩码** | Key 回读时只显示尾部若干位，任何响应与表单都不回显明文 |
| **stub 模式** | 没有任何云端 Key 时的兜底实现：全链路可跑，结果用于试用与联调，不追求真实质量 |

**措辞纪律**：stub 模式不是「演示模式」「离线模式」，就说 **stub 模式**。

## 9. 工程语境里对教师可说的词

| 术语 | 对教师怎么说 |
| --- | --- |
| 用户 | **教师**（这是单一用户产品，没有「账号」「成员」） |
| 接口 / API | 不需要出现在界面文案里 |
| 版本树 / 版本号 | 「历史版本」+「第 N 版」对教师更直观 |
| 冲突检测 | 「发现矛盾，等你裁决」 |
