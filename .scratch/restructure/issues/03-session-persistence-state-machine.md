# 03: 备课会话持久层+状态机收编

**What to build:** 后端成为备课会话的唯一事实源:会话与消息持久化,教师换设备、清浏览器数据后历史不丢;对话状态机(澄清→检索→生成→反馈)从 api 路由收进 core,意图按会话增量累积(不再每轮全量重析),「跳过追问」改为语义化判定(不再依赖硬编码子串词表)。api 层只剩参数校验与转发(ADR-0002)。

**Blocked by:** 02 RAG 策略收编

**Status:** ready-for-agent

- [ ] 会话 API:创建/列表/历史/重命名/删除,全部经 HTTP 缝测试
- [ ] 同一会话多轮对话意图增量累积,行为有测试对比
- [ ] 跳过追问为语义化判定,行为有测试
- [ ] 澄清/追问判断逻辑不再出现在 api 层
- [ ] 测试只断言 API 响应与数据变迁,不断言内部调用
