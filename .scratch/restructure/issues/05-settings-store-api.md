# 05: 设置存储+设置 API

**What to build:** 配置从「改 .env 重启」升级为「API 写穿即时生效」:供应商/模型名/API Key/任务级模型路由(意图分析/生成/冲突比对三档)存服务端配置库,覆盖 .env 引导默认;一切回读脱敏。为设置界面(工单 15)供数据(ADR-0003)。

**Blocked by:** 01 能力注册统一

**Status:** ready-for-agent

- [ ] 配置库优先于 .env,写入后新请求立即生效(无重启)
- [ ] 按任务选模型,未设置的任务回落全局默认,行为有测试
- [ ] API 回读的 Key 一律掩码,原始 Key 不出现在任何响应
- [ ] 无效配置(未知供应商/模型)返回明确错误码与信息
