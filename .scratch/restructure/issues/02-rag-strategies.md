# 02: RAG 策略收编

**What to build:** 检索与分块成为可插拔策略:Retriever(纯向量 / 向量+图谱融合两个实现)与 Chunker 接口化并纳入能力注册,检索逻辑从编排器移入策略实现,默认行为不变。后续升级 RAG(加重排、换分块方式)只动策略,不动引擎躯干(ADR-0003)。

**Blocked by:** 01 能力注册统一

**Status:** ready-for-agent

- [ ] Retriever/Chunker 接口 + 两实现,配置按名切换
- [ ] 编排器不再内嵌检索细节,只消费策略结果
- [ ] 默认配置下检索行为与收编前一致(既有图谱检索测试保持绿)
- [ ] 两种检索策略的行为差异经 API 可观测并有测试
